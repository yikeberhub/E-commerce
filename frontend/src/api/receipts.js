export const generateReceipt = (orderId, vendorId, transactionId) => {
  // Logic to create a receipt (e.g., generate PDF or save in database)
  console.log(
    `Generating receipt for Order ID: ${orderId}, Vendor ID: ${vendorId}, Transaction ID: ${transactionId}`
  );
};
